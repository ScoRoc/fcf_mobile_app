// Constants
import { NAV } from 'utils/constants';

const wodTools = {
  // in display order, so don't alphabetize
  unitConverter: {
    // iconColor: 'white',
    iconLibrary: 'MaterialCommunityIcons',
    iconName: 'calculator-variant',
    label: 'Unit Converter',
    // labelColor: 'white',
    toScreen: NAV.UNIT_CONVERTER,
  },
  percentTable: {
    iconLibrary: 'Feather',
    iconName: 'percent',
    label: 'Percent Table',
    toScreen: NAV.PERCENT_TABLE,
  },
};

export default wodTools;
